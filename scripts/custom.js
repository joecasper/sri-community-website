jQuery(document).ready(function ($) {
    // Home Page Slider
    $carousel = $('#featured-slider').flickity({
        cellSelector: '.slide',
        setGallerySize: false,
        wrapAround: true
    });
});